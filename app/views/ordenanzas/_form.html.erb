<%= form_for(@ordenanza) do |f| %>
  <% if @ordenanza.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ordenanza.errors.count, "error") %> prohibited this ordenanza from being saved:</h2>

      <ul>
      <% @ordenanza.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
