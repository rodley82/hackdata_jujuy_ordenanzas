<p id="notice"><%= notice %></p>

<h1>Listing Ordenanzas</h1>

<table class="table table-hover table-striped">
  <thead>
    <tr>
      <th>Numero</th>
      <th>Año</th>
      <th>Tema</th>
      <th>Vigente</th>
      <th>Unidad</th>
      <th>Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @ordenanzas.each do |ordenanza| %>
      <tr>
        <td> <%= ordenanza.numero %> </td>
        <td> <%= ordenanza.anio %> </td>
        <td> <%= ordenanza.tema %> </td>
        <td> <%= vigente?(ordenanza) %> </td>
        <td> <%= ordenanza.unidad_id %> </td>
        <td> 
          
          <%= link_to ordenanza,class: "btn btn-primary" do %> 
            Ver <span class="glyphicon glyphicon-zoom-in" aria-hidden="true"></span>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ordenanza', new_ordenanza_path %>
